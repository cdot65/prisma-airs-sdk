# September 7, 2026 release delivery

The user explicitly authorized pushing and publishing the implemented SDK/CLI surface with the remaining AI Gateway gaps disclosed. This supersedes the earlier decision to withhold all releases; it does not change the original 99% goal or turn failed E2E workflows into passes.

## Grouped analytics delivery — SDK 0.25.0 and CLI 4.4.0

### Published registry verification (supersedes candidate status below)

SDK **0.25.0** is published from `9dc8690c24fd11fb0abc51a1233e1ecb7968fe6b`. [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34117836857), the [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34117836858), [Docusaurus deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34117836860), and [npm publication](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34118335579) pass. The annotated release tag and main commit are on the remote; no earlier tag was moved.

A fresh npm installation passes all nine payload/source-map/type/module/native/authentication checks at **2026-09-07T11:52:23.865Z**. At **2026-09-07T11:58:25.132Z**, an independent registry check binds the installed lockfile and verified archive to npm's current SHA-512 integrity. The package is **1,287,407 bytes**, SHA-256 **`bed2bd1779703beb88820b46b4866b7af68cb053a83d1dfd91cba4f1747a34a4`**, identical to the final tested candidate. The first metadata checker expected an object; this npm version returns a one-element array. The corrected checker requires exactly one result and retains every URL/version/hash assertion; the failed attempt is preserved.

The registry-installed SDK passes **102/102 grouped-filter checks at 2026-09-07T11:52:02.500Z**, **53/53 existing chart contracts at 2026-09-07T11:52:36.044Z**, and **10/10 inference checks at 2026-09-07T11:51:36.327Z**. Independent retirement confirms **33/33** historical release keys absent at **2026-09-07T11:52:17.226Z**. Credential configuration is unchanged. The new CLI 4.4.0 candidate pins this published SDK and is undergoing separate consumer verification; it is not yet claimed published. These successful scoped checks do not change the full-project **5/10** assessment or the **138/242** gateway denominator.

### CLI 4.4.0 final registry, user-install and container verification

This supersedes the pending CLI status in the earlier SDK checkpoint above. CLI **4.4.0** is published from `d7630332313147f56dfcc15f7cac426f5a7a18f4`, with source and annotated tag on both remotes. [All seven CI jobs](https://github.com/cdot65/prisma-airs-cli/actions/runs/34121162969), [npm publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34121364375), and [all four container jobs](https://github.com/cdot65/prisma-airs-cli/actions/runs/34121350201) pass. The global user-prefix `airs` reports **4.4.0** and uses SDK **0.25.0**.

The fresh registry installation passes **103/103 groups at 2026-09-07T12:32:16.444Z**, **54/54 charts at 2026-09-07T12:35:21.598Z**, **8/8 inference at 2026-09-07T12:29:23.185Z**, **3/3 empty-window checks at 2026-09-07T12:29:09.610Z**, and **11/11 native DLP at 2026-09-07T12:26:53.881Z**. The upgraded user installation passes **12/12 cross-service reads/scan at 2026-09-07T12:29:31.518Z** and **11/11 native DLP at 2026-09-07T12:29:11.324Z**. Independent retirement confirms **36/36** historical keys absent at **2026-09-07T12:30:44.576Z**; the subsequent read-only chart rerun creates no resources. Credentials remain unchanged.

Both consumers match all seven package files, five distribution files and 19 exports. At **2026-09-07T12:28:22.542Z**, npm metadata, the installed lockfile and the reviewed archive agree: **123,664 bytes**, SHA-256 **`f53c31400945a4004d15c5a7368f97dae6101c8ccbb97d71f5821dbf86dcb7c1`**. Frozen and fresh-registry production audits report zero advisories. Node 20.17/22.13/24 native consumer CI remains passing.

Actual container checks pass **11/11 amd64 at 2026-09-07T12:24:43.928Z** and **11/11 arm64 at 2026-09-07T12:25:23.710Z**, without runtime network or credentials, using packaged fonts. The verifier binds workflow success to the release commit, validates both native JSON reports and confirms minor/latest readbacks at **`sha256:6134107b600e76491a70bcbdc261337abe9d53673082c1cc1a058676c87e6b52`**. Host DLP checks use the disclosed process-only font configuration; temporary corpora are removed before success.

The first registry chart run retains **53/54** with one masked subprocess failure; its root cause is not established. A complete serialized rerun passes all 54. Fourteen new offline harness tests verify secret-safe HTTP/exit/timeout classifications without retaining child arguments or raw diagnostics; SDK runtime and retry policy are unchanged. The first public CLI candidate check retains **10/11** with DNS resolution failure; its complete rerun passes **11/11 at 2026-09-07T12:24:01.380Z**. Final registry-capture documentation deployment is verified separately below. Registry visibility lag caused no duplicate publication.

The original 99% full-spec target remains unmet: AIRS **149/149**, direct Gateway **138/242 (57.02%)**, all **22** analytics adaptations still partial, and the full-project assessment remains **5/10**. Existing failed service/provider/entitlement workflows, retained unbound DLP fixture, SDK documentation dependency advisories, WAN and anonymous container access limits remain open. Successful release delivery is not a full-project sign-off.

### Final documentation verification

CLI registry-output documentation commit `b8edcf52e1ccabb9d2a63ea803d7159890b8fda7` is on both remotes and passes [CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34123813148) and [deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34123813106). Public browser checks pass **11/11 at 2026-09-07T12:49:57.332Z**, comparing actual registry inference, empty-window, grouped/chart-filter and native DLP JSON. Local CLI browser checks pass **10/10 at 2026-09-07T12:45:38.034Z**. The existing isolated browser disables its sandbox explicitly and receives no live credentials; request origins remain restricted.

The final verification-only checkout subsequently passes **11,299 tests in 144 files**, retaining **99.71% lines/statements, 100% functions and 96.67% branches**. Typecheck, tooling typecheck, lint and format pass; the published SDK runtime is unchanged. The following candidate history keeps its original counts.

Both Docusaurus builds pass. SDK HTTP preview verifies **17 pages and four assets at 2026-09-07T12:52:21.173Z**, including all **71** suite timestamps and the final unit count. A complete source, artifact, built-site, registry-package and user-installed CLI scan at **2026-09-07T12:52:22.798Z** checks **18,332 files**, detects zero credential leaks and confirms unchanged credential configuration. Vault indexing changes no catalogs; checking passes with **3,691 notes, 471 directories, zero errors and 1,065 retained review warnings**.

### Earlier CLI 4.4.0 candidate integration

The CLI pins the already-published SDK through pnpm and is versioned with Changesets. All six `group-by` dimensions now accept the nine filters, with SDK-owned dimension/column constants and filter validation before client creation. Invalid dimensions and user columns produce usage exit 2; JSON/YAML envelopes and cents remain unchanged. All **95** new command/transport regressions fail first, then pass after implementation and correction of a missing `object` field in the test fixture. The production schema is not weakened. Full CLI verification passes **1,268 tests in 92 files**, **95.97% lines/statements, 95.48% functions and 87.34% branches**, plus **14** release-policy/diagnostic tests, lint, formatting, typecheck and a clean production audit. Initial tests emitted host fontconfig diagnostics; the repeat uses the existing process-only font configuration and passes cleanly.

Review preserves the existing tracked package verifier's archive path protections and exact 19-export assertion, adding group help, hashes and strict diagnostic handling. An initial pnpm-packed archive rewrites package metadata and fails byte comparison, so the release candidate uses the same npm packing behavior as publication; the earlier archive is retained. The final archive has **123,664 bytes**, SHA-256 **`f53c31400945a4004d15c5a7368f97dae6101c8ccbb97d71f5821dbf86dcb7c1`**. All seven payload/five distribution files match the independently installed candidate.

Exact npm-built candidate checks pass **103/103 groups at 2026-09-07T12:07:30.217Z**, **54/54 charts at 2026-09-07T12:12:02.321Z**, **8/8 inference at 2026-09-07T12:10:57.358Z**, **11/11 native DLP at 2026-09-07T12:10:40.979Z**, and **12/12 cross-service reads/benign scan at 2026-09-07T12:12:38.879Z**. Empty-window JSON/YAML passes **3/3**. Independent retirement passes **35/35 at 2026-09-07T12:13:37.774Z**. The first grouped candidate run records **102/103**, with a provider/status-code query failure; the full rerun passes without changing validation, retries or infrastructure. The initial masked diagnostic does not establish the cause, and uninterrupted service availability is not claimed.

Versioned reports preserve earlier CLI captures and now validate CLI identity, exact dependency pin and actual installed SDK before a new inference key can be created. The cross-service suite also receives versioned reporting; an initial unversioned run is retained in history and the original 4.3.1 canonical report is restored from its unchanged historical file. New harness tests bring the SDK verification checkout to **11,285 tests in 143 files** without modifying the published runtime. Registry CLI, container and user-prefix promotion are still pending, as is final public documentation verification.

### Earlier SDK candidate history

The next implementation extends the nine verified filters to all six SCM grouped analytics endpoints: `ai_service`, `model`, `api_key`, `provider`, `status_code` and `users`. It reuses the chart validation and serializer, preserves existing columns and both response envelopes, and rejects unsupported fields, coercions, unsafe values and reversed bounds before authentication. User grouping does not acquire unverified column options. No new directly matched upstream operation is claimed; AIRS stays **149/149**, gateway **138/242**, and all 22 analytics adaptations remain partial. The full-scope assessment remains **5/10**.

Read-only discovery passes **13/13** trace/metadata checks at `2026-09-07T11:02:06.491Z`, then **97/97** range/list/intersection checks at `2026-09-07T11:03:39.333Z`. Source SDK independently passes **97/97** at `2026-09-07T11:07:51.141Z`. The final raw SCM suite passes **102/102** at `2026-09-07T11:10:38.954Z`, including exact filtered cost/token columns on non-user groups. The independently installed candidate passes **102/102** at `2026-09-07T11:15:59.683Z`. All use historically owned positive traffic and empty negative cohorts, create no resources and preserve the credential file.

The 295 new group regressions initially report **72 failures / 223 passes**. Implementation closes the filter failures; a test fixture's missing snake-case quota flag is corrected without weakening the response schema. Four independent source-contract tests preserve all upstream user/model/provider query declarations. The provider specification omits `trace_id`, so its live-verified SCM `traceId` is recorded as an extension, not fabricated upstream coverage. Nineteen release-harness regressions additionally protect package loading before credential/key creation and version-independent retirement inventory. Full verification passes **11,252 tests in 141 files**, with **99.71% lines/statements, 100% functions and 96.64% branches**.

The earlier candidate tarball is **1,286,806 bytes**, SHA-256 **`1b287ffefea2cf83a159b354d6ec0fb6ae1e5e67bf22fd8846c8baab1c599d12`**. At `2026-09-07T11:16:35.767Z`, its installed consumer matches all nine payload files, 126 source-map sources and 1,304 runtime exports. ESM/CommonJS checks include exact group queries, pre-authentication rejection, native WebSocket exchanges and actual OAuth timeout/recovery. Strict NodeNext types now use a tracked, hashed specimen in an owned temporary sibling of the installed package, with cleanup in `finally`; an empty caller-supplied smoke file can no longer substitute for the reviewed type contract.

Installed-candidate inference passes **10/10** at `2026-09-07T11:21:37.480Z`, using only the prescribed models and a temporary key retired during cleanup. The independent read-only inventory confirms **31/31** historical release keys absent at `2026-09-07T11:23:06.772Z`. The production audit is clean; SDK documentation-only dependency advisories remain open. The `2026-09-07T11:23:33.588Z` secret scan checks **18,354 files**, detects no leaks and verifies unchanged configuration. These are candidate checks, not yet registry publication or CLI group-filter certification. Final remote/public release evidence is recorded after those gates pass.

### Pre-publication self-review: serialize only validated values

Review finds that chart/group/log serializers reread caller-owned properties after validation. A getter can return a different value or throw on that second read. All **11** new single-read tests fail before the fix. Window validation now returns its parsed copy alongside serialized time parameters, and endpoint-specific filters/columns are serialized from that copy. Validation still runs once, with no new public export or widened unrelated query schema. A missing `capturedTotal` in the log test fixture is corrected without changing the production response schema. All 11 regressions and the existing telemetry tests pass; full coverage passes **11,263 tests in 142 files**, **99.71% lines/statements, 100% functions and 96.67% branches**.

The superseding tarball is **1,287,407 bytes**, SHA-256 **`bed2bd1779703beb88820b46b4866b7af68cb053a83d1dfd91cba4f1747a34a4`**. Independent installed-consumer verification passes at `2026-09-07T11:30:45.617Z`, with the same nine files, 126 source-map sources, 1,304 exports, both module formats and all native/type/authentication checks. This exact candidate passes **102/102** groups at `2026-09-07T11:30:23.092Z`, **53/53** existing chart contracts at `2026-09-07T11:31:15.013Z`, and **10/10** inference at `2026-09-07T11:29:54.106Z`. The independent retirement inventory confirms **32/32** historical release keys absent at `2026-09-07T11:30:54.151Z`. Earlier archives and failed attempts remain preserved. The original 0.24.0 chart evidence is not overwritten: new chart checks use versioned report names.

## Hardening release — CLI 4.3.1, SDK 0.24.0 unchanged

CLI 4.3.1 is cut with Changesets and published from `d00f435ba8b719472bd4a9f2c9e5305ee0bf6fdb`; source and tag are on both remotes. [All seven CI jobs](https://github.com/cdot65/prisma-airs-cli/actions/runs/34110678594), including native consumers on Node 20.17/22.13/24, and [npm publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34110816293) pass. SDK runtime code is unchanged, remains published as 0.24.0 and stays exactly pinned by the CLI. The original full-spec goal remains active and the full-scope self-assessment remains **5/10**, not 9/10.

### Implemented review findings

- **Production dependency remediation:** js-yaml 4.3.2, nanoid 5.1.16 (direct and DOCX), and sharp 0.35.4 remove the known production advisories in both the frozen pnpm tree and the independently installed npm tree. The new audit gate runs in CI, npm publication/prepublish and container publication. No advisories were suppressed or unrelated major upgrades applied. SDK documentation-only image-size advisories remain unresolved; npm still lists 2.0.2 as latest.
- **DLP automation:** failing-first public-command tests record 21 failures / 9 passes before the fix and 30/30 afterward. Shared output resolution now honors JSON/global/environment/config precedence. Count and seed validation rejects partially parsed, fractional and unsafe values before writes. Renderer separation keeps JSON pure and retains per-format data in quiet pretty output. Sixteen runtime-version checks align package metadata and doctor with the existing dependency engine intersection, `^20.17.0 || ^22.13.0 || >=23.5.0`.
- **Release ordering:** exact version tags are published without implicit `latest`. Both architectures execute the native consumer smoke before a short serialized promotion job rereads current stable tags. Numeric-version comparison allows current minor maintenance releases without stealing `latest`; superseded patches promote neither. Shared alias digests are read back and checked. Older workflow revisions and independent manual writers do not retroactively acquire this guard.
- **Native environment and build context:** the Alpine runtime includes fontconfig/DejaVu; only required Docker build inputs enter the context. The isolated smoke owns a temporary font cache and leaves the user's home/configuration alone. This host uses its existing process-only font configuration. Two initial CI attempts exposed cache isolation and the legitimate Node 20.17 JSON-module warning; both failures are retained. The warning is now explicitly recorded and narrowly recognized, with four diagnostic regressions; unexpected/mixed errors still fail. Runtime warnings are not disabled.

### Independent delivery verification

The fresh registry package and the upgraded user-prefix `airs` match all **seven package files**, **five distribution files**, **19 library exports**, version/help and the SDK pin. Their package checks pass at `2026-09-07T10:29:21.883Z` and `2026-09-07T10:30:42.329Z`. The tarball is **123,393 bytes**, SHA-256 **`d6e828d8c925c26e01102e6c7a2663d94686f44a3ed3ef1af67552b087126876`**; its SHA-512 matches npm publication metadata.

Registry validation passes **54/54** chart contracts at `2026-09-07T10:32:02.882Z`, **8/8** inference at `2026-09-07T10:30:56.976Z`, **3/3** empty-window JSON/YAML at `2026-09-07T10:31:01.470Z`, and **11/11** native DLP checks at `2026-09-07T10:29:23.947Z`. Native generation validates five formats, 26 signatures and the manifest, with sharp 0.35.4 / libvips 8.18.6. The user-installed CLI also passes the native suite and **12/12** cross-service reads/benign scan at `2026-09-07T10:32:01.368Z`. Independent retirement confirms **30/30** historical release keys absent at `2026-09-07T10:32:02.499Z`. No runtime settings or credential configuration were changed.

The [container workflow](https://github.com/cdot65/prisma-airs-cli/actions/runs/34110815270) passes all four jobs. Actual container execution passes **11/11 amd64** at `2026-09-07T10:24:14.773Z` and **11/11 arm64** at `2026-09-07T10:25:02.738Z`, with Node 20.20.2, no runtime network/credentials and no custom font configuration or warnings. Both `4.3` and `latest` are verified at **`sha256:c113c58d457e0152b5f51c1cab850d771b7ddeac9ca88d8020b74442791cfadf`** after promotion. This is remote runtime evidence, not a claim of local Docker execution or anonymous public pull access.

npm explicitly reported package processing after accepting publication. Version metadata appeared before the tarball; the first attempts failed with ETARGET and then tarball 404. No downstream inference or key creation ran during those installation failures. The normal registry tarball became available before the independently installed passing checks above; no republish, registry policy change or local-tarball substitute was used for registry certification.

Current SDK verification passes **10,934 tests in 137 files**, with 99.71% lines/statements, 100% functions and 96.55% branches. CLI passes **1,173 tests in 91 files**, plus **14 release-policy/diagnostic tests**; its library coverage remains 95.97% lines/statements, 95.48% functions and 87.35% branches (CLI rendering is excluded by its existing coverage configuration). Seven pre-existing lint warnings remain. Fresh source-backed OpenAPI auditing retains AIRS **149/149** and gateway **138/242 direct (57.02%)**, including 22 partial analytics operations. These results do not erase the documented service/model/entitlement failures, unretired DLP fixture, or missing upstream operations. The next acceptance work is the remaining full-source contracts and service-backed lifecycle gaps, not further cosmetic scoring changes.

### Final public delivery

SDK documentation/evidence commit `1ffc414bad17a3ee64f11459b77074081aabfc59` is pushed and passes [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34113203058), the [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34113203050) and [Docusaurus deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34113203056). CLI documentation commit `26a331ef60ebafb67a9c9e945555aa416a72d249` is pushed to both remotes and passes [CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34112721571) and [deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34112721610).

Public Chromium checks pass **8/8 SDK** at `2026-09-07T10:52:40.255Z` and **11/11 CLI** at `2026-09-07T10:45:07.083Z`. They compare actual registry inference, empty-window, chart-filter and native DLP JSON captures, not merely page availability. Local browser checks pass 6/6 SDK and 10/10 CLI; SDK HTTP preview verifies 17 pages and four assets. The documented browser isolation disables its sandbox but loads no live credentials and restricts requests to documentation/font origins. Both owned loopback preview servers are stopped afterward.

The final source, artifact, built-site, SDK/CLI registry-package and user-installed CLI scan at `2026-09-07T10:52:48.885Z` checks **18,296 files**, finds zero credential leaks and confirms unchanged credential configuration. Vault indexing makes zero catalog changes; validation reports **3,686 managed notes, 471 directories, zero errors and 1,058 retained review warnings**. Concurrent vault content is preserved. The remaining full-scope work and **5/10** assessment above are not changed by successful patch delivery.

## Query-contract release — SDK 0.24.0 and CLI 4.3.0

SDK 0.24.0 is published from `28b41fec71436e8c2a6d162272da686b0c79a737`. [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34101940006), the [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34101940088), [documentation deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34101940111), and [npm publication](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34102604991) pass. Registry availability and archive integrity are independently checked. The registry package passes **53/53** read-only chart contracts at `2026-09-07T08:56:03.088Z`, **10/10** inference checks at `2026-09-07T08:56:49.058Z`, and exact-payload/types/ESM/CommonJS/source-map/OAuth/WebSocket checks at `2026-09-07T08:56:39.592Z`. All nine payload files, 126 source-map sources and 1,304 runtime exports match. Its archive SHA-256 remains `9c5eed823b6751bb80d875d10524c4e33739f445cf308f7572c7ffeec45e8bb9`.

CLI 4.3.0 is a Changesets minor release, pinned to the already-published SDK 0.24.0. Source and tag `055b740a80ef2ea0ed1f9bd49345356bd436dd3f` are pushed to both GitHub and origin; [remote CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34104584621), [documentation deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34104584671), [npm publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34104767214), and [container publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34104765159) pass. The container digest is `sha256:0776cbec1ea9df248ab2d5c0b9b20679da0b54e95931cc00be4039c0012a7c00`, tagged `4.3.0`, `4.3` and `latest`. Local container execution/public pull remain unverified; publication is not runtime certification.

The independently registry-installed CLI passes **54/54** chart contracts at `2026-09-07T09:20:57.909Z`, **8/8** inference at `2026-09-07T09:19:53.288Z`, and **3/3** empty-window JSON/YAML at `2026-09-07T09:19:46.233Z`. Registry payload verification passes at `2026-09-07T09:18:53.661Z`. The upgraded user-prefix CLI passes the same seven-file/19-export/version/help verification at `2026-09-07T09:20:49.055Z` and **12/12** cross-service reads/benign scan at `2026-09-07T09:21:00.488Z`. The independent all-history audit confirms **28/28** release-inference keys absent at `2026-09-07T09:20:51.289Z`. Credential configuration remains unchanged.

An npm metadata-cache lag initially prevented package installation. The downstream checks failed because the executable did not exist; the one temporary key from that failed inference attempt was retired. The failed checks remain in history. Retrying installation with refreshed registry metadata succeeded, and all subsequent registry checks above pass. Three new harness regressions ensure absolute/readable CLI entry validation precedes the key lifecycle; the current SDK verification checkout passes **10,934 tests in 137 files**, at 99.71% lines/statements, 100% functions and 96.55% branches. This is a verification-only follow-up, not an untested runtime change to SDK 0.24.0.

The CLI now exposes the nine verified filters on requests, cost, tokens and latency, with SDK-owned validation before client creation or cost workspace resolution. It preserves inclusive bounds, zero, fractional cents, list OR/intersection behavior, null latency and the existing cost renderer. Review also corrected partially parsed day counts and UUID-to-slug resolution for cost queries. Other chart/group filters remain unexposed. **92 new regressions** bring the CLI to **1,127 tests in 90 files**, with 95.97% lines/statements, 95.48% functions and 87.35% branches. Lint, format, strict typecheck, frozen installation and Docusaurus build pass; seven pre-existing lint warnings remain. Generated API references are refreshed rather than suppressed.

The final packaged CLI passes all seven payload/five distribution-file comparisons, 19 library exports and help/version checks at `2026-09-07T09:10:46.728Z`. Its executable passes **54/54** chart contracts at `2026-09-07T09:12:31.997Z` and **12/12** cross-service reads/benign scan at `2026-09-07T09:11:27.031Z`. The preceding candidate, before the workspace-help wording correction, passed **8/8** inference at `2026-09-07T09:05:56.688Z`, **3/3** empty-window checks at `2026-09-07T09:05:49.581Z`, and **54/54** chart checks at `2026-09-07T09:07:04.718Z`. All **26** historical release-inference keys are independently confirmed absent at `2026-09-07T09:07:53.008Z`. Read-only chart tests create no fixtures or traffic.

Initial test failures exposed two test-only assumptions: analytics status codes must follow the SDK's nonnegative integer domain, not an invented HTTP range; and restored mock constructors must remain constructible across tests. The corrected tests retain the full schema/output/no-auth assertions. The first registry SDK inference invocation omitted the harness's explicit `--writes` switch and failed before creating a fixture; its corrected authorized run passes and the failed attempt remains in history. A local browser launch failed before navigation because the downloaded glibc binary is incompatible with this musl environment; the existing isolated Chromium launcher passes the actual page checks without weakening assertions. Vault validation catches and corrects two missing `/index` effort links in the new concept, then passes with **3,679 notes, 470 directories, zero errors and 1,049 retained warnings**.

Full-project self-assessment remains **5/10**. This increment improves verified behavior and release reliability, not the denominator: AIRS remains 149/149 modeled operations; AI Gateway remains **138/242 direct (57.02%)**, with 22 partial analytics adapters. Existing provider/service/entitlement failures, the unretired DLP fixture, WAN and container-runtime limits remain explicit. The older 4.2.2 publisher is subsequently cancelled for the verified latest-tag collision described below, not because it was slow. No production permissions, security policy, model selection or credential configuration was weakened to obtain green checks.

### Final public delivery and publication ordering

SDK evidence commit `a6fe71a64076e0c21a3b8279878d63e063a85751` is pushed and passes [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34106353617), the [complete Node matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34106353629), and [Docusaurus deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34106353698). CLI evidence commit `40be9d2b8463133c721403851fe1e3012496f90e` is pushed to both remotes and passes [CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34106234076) and [deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34106234121). Real public Chromium checks pass **8/8 SDK** at `2026-09-07T09:36:06.840Z` and **11/11 CLI** at `2026-09-07T09:30:41.623Z`, including exact registry output, all 52 chart-filter result pairs and the newly disclosed DLP JSON-summary failure. Browser isolation and the explicitly disabled sandbox remain documented. Both owned preview servers are stopped afterward.

The final source/artifact/built-site/registry/user-installed scan at `2026-09-07T09:39:19.813Z` checks **18,170 files**, detects no credential leaks and confirms unchanged configuration. Vault indexing reports zero changes; checking reports **3,681 managed notes, 470 directories, zero errors and 1,051 retained review warnings**. Concurrent unrelated vault content is preserved.

A final ordering check proves the older 4.2.2 container run can overwrite the same `latest` tag: its exact tagged workflow uses semver metadata with implicit automatic latest-tag generation, as documented by [metadata-action v5](https://github.com/docker/metadata-action/tree/v5#latest-tag). Run `34096222716` is therefore explicitly cancelled as a superseded publisher, and its completed/cancelled status is confirmed. This is a verified tag-collision mitigation, not cancellation because of elapsed time or missing logs. Only after it terminates is the already-passing 4.3.0 container workflow rerun to reassert current tags; npm releases and Git tags are not changed. A durable monotonic alias-promotion guard remains a release-pipeline follow-up.

Reassertion attempt 2 passes at `2026-09-07T09:41:28Z`, publishing `4.3.0`, `4.3` and `latest` at digest **`sha256:d673943af4ec1c2c99893005d1f5a7801030cc534befe137dad5e192980d2cba`**. The earlier digest above remains the first publication's historical result. Logs from the cancelled 4.2.2 run confirm it had remained in the ARM64 dependency-install stage; it is not reported as a passing publication. A post-reassertion credential scan at `2026-09-07T09:42:32.027Z` checks **18,175 files**, detects no leaks and verifies unchanged configuration. No local Docker execution or public pull certification is inferred from a successful push.

### Newly identified CLI dependency follow-up

The separate CLI production-lockfile audit reports **five high and one moderate advisory** across `js-yaml` 4.1.1, `nanoid` 5.1.6 (direct and via docx), and optional `sharp` 0.34.5. This is not the SDK's clean production audit and must not be described as one. Reviewed advisories identify fixes in [js-yaml 4.3.1](https://github.com/advisories/GHSA-5p4m-2wfm-xmqj), [nanoid 5.1.16](https://github.com/advisories/GHSA-28wg-ghj8-5hjv), and [sharp 0.35.x with patched libvips](https://github.com/advisories/GHSA-f88m-g3jw-g9cj). The CLI uses raw internally generated pixel buffers with sharp; this does not erase the dependency advisory or certify every consumer. npm range resolution and the CI lockfile are different dependency trees and require separate audits. A focused follow-up must update the supported dependency lines, verify native image generation and YAML behavior, enforce a production-audit release gate, and document any Node compatibility change. No audit suppression or broad major-version upgrade has been applied merely to obtain a green result.

Fresh registry installation confirms only the optional sharp advisory remains, propagated to the parent CLI package (two high audit entries for one affected dependency). A local user-installed DLP generation smoke successfully produces five PNG and five JPEG files, with independently checked signatures at `2026-09-07T09:24:03.285Z`; no install-script allowance or global npm policy was changed. This extra check also exposes an existing automation defect: `runtime dlp generate --output json` emits a human-readable summary, not JSON. Native image generation passes, but the JSON contract fails and belongs in the same focused follow-up. Do not report this as an all-green full CLI E2E run.

The next self-review iteration has three concrete acceptance gates: update supported dependency lines and require a clean production audit before publication; reproduce and fix DLP summary output precedence with native file-generation regression checks; and prevent older container releases from promoting shared aliases. Preserve the original upstream denominator and every retained service failure throughout. These are outstanding implementation tasks, so the full goal remains active and the score is not inflated to 9/10.

### Discovery and candidate history

The full-scope review found seven additional, verifiable SCM query options: status-code/API-key/provider-model lists and inclusive total-token/cost bounds. The initial read-only discovery retains **11 pass / 14 fail** across controls and 22 hypotheses at `2026-09-07T08:08:38.401Z`. Upstream snake-case names were ignored; several prompt/completion-token bounds failed their controls. These observations are not reclassified as passing API operations.

The focused raw SCM contract run passes **53/53** at `2026-09-07T08:12:49.467Z`; the packed SDK 0.24.0 candidate independently passes **53/53** at `2026-09-07T08:23:51.320Z`. Each of four charts verifies singleton and CSV-OR lists, inclusive lower/upper bounds, equal-bound ranges and combined-filter intersections against one historically owned positive inference trace, with empty negative cohorts. Credentials are unchanged; no inference, key, log or environment mutation is performed by these analytics checks.

Failing-first regression testing records **40 failures / 208 passes** before implementation and all **248 cases passing** after it. The full local SDK suite passes **10,916 tests in 135 files**, with 99.71% lines/statements, 100% functions and 96.55% branches. Strict scalar/list validation, zero/fractional-cent preservation, safe query encoding and reversed-range rejection precede authentication. Independently source-hashed adapter fixtures retain all upstream declarations and their explicit partial classification.

The initial packed-consumer verification passes at `2026-09-07T08:24:34.703Z`: all nine payload files, 126 source-map sources, 1,303 runtime exports, strict consumer types, actual OAuth deadline/recovery and native WebSocket exchanges in ESM/CommonJS. That initial archive is preserved privately at SHA-256 `a60336d35f6a4a01bc4cf807ee4f79029429405a8ae3b153d8f8df944150a091` (1,284,208 bytes).

The final candidate additionally exports `AIGatewayChartFiltersSchema` and its inferred type, sharing field/range definitions with the SDK transport for CLI validation before workspace resolution or authentication. All 15 standalone-schema checks fail before the export and pass after it. The final suite passes **10,931 tests in 136 files** at the same coverage thresholds. Final packed live contracts pass **53/53** at `2026-09-07T08:36:44.862Z`; both-format consumer verification passes at `2026-09-07T08:37:24.322Z`, now covering **1,304 runtime exports**. Final archive SHA-256 is `9c5eed823b6751bb80d875d10524c4e33739f445cf308f7572c7ffeec45e8bb9` (1,286,162 bytes). The 08:37:58 UTC source/artifact/built-site/candidate scan checks **16,786 files**, finds no credential leaks and confirms unchanged configuration. Registry publication and final deployment are recorded only after they occur.

Direct coverage remains **138/242 (57.02%)**, all 22 analytics adaptations remain partial, and full-scope self-assessment remains **5/10**. Prompt/completion-token filters, other chart/group contracts and all previously recorded service/provider/entitlement failures remain unverified or failing. The 4.2.2 container job is still running; its observation timeout has not been treated as termination or permission to restart it.

## Delivered telemetry correction — SDK 0.23.0 / CLI 4.2.2

[SDK v0.23.0](https://github.com/cdot65/prisma-airs-sdk/releases/tag/v0.23.0) is published from `6d17306e083dbf9779700af3d72c084fa7cf7acf`: [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34095014453), [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34095014430), [publication](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34095254807) and [initial documentation deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34095014412) pass. [CLI v4.2.2](https://github.com/cdot65/prisma-airs-cli/releases/tag/v4.2.2) is published from `193b995ea17f6ec9862e613b6b4dadda5da12de1`, on both remotes: [all four CI jobs](https://github.com/cdot65/prisma-airs-cli/actions/runs/34096066301) and [publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34096236940) pass. Changesets cut the CLI patch only after SDK publication, with an exact manifest/lockfile pin.

Registry SDK inference passes **10/10** at `2026-09-07T07:28:57.826Z`; registry SDK analytics passes **13/13** at `2026-09-07T07:28:54.307Z`. Registry CLI inference passes **8/8** at `2026-09-07T07:39:58.210Z`, and empty-window JSON/YAML checks pass **3/3** at `2026-09-07T07:39:51.294Z`. The upgraded user-installed CLI passes **12/12** cross-service reads/benign scan at `2026-09-07T07:40:08.790Z`. Independent all-history key retirement passes **24/24** at `2026-09-07T07:40:49.758Z`, after both release-inference runs. Config remains unchanged; no runtime setting is persisted.

SDK registry verification passes at `2026-09-07T07:29:42.618Z`: all nine payload files, 126 source-map files, 1,303 runtime exports, strict types, real OAuth deadline recovery and native WebSocket exchanges in both formats. SHA-256 is `f7eb5217a710b917748083bf7eb8d18dfb5373eca312317589882438cd7c64b3`; registry integrity matches the candidate. CLI registry and user-installed payloads match all seven files, including five distribution files, and all 19 library exports/version/help pass. CLI full coverage passes **1,035 tests in 88 files**, with 95.96% lines/statements, 95.48% functions and 87.29% branches; seven pre-existing lint warnings remain. A restored test mock initially broke the second YAML case; isolating configuration with a plain async function resolves that test-only lifecycle issue without changing assertions or runtime behavior.

The final registry captures are deployed on both documentation sites. SDK source `5166ef8714ee24b5c938322103757d16c224061b` passes [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34097239366), the [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34097239413) and [Docusaurus deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34097239368). CLI source `3baff939416288dc5537319d4be3d4ffb332fb91` is pushed to both remotes and passes [CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34096964579) and [deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34096964552).

Real Chromium public-page checks pass **8/8 SDK** at `2026-09-07T07:55:28.967Z` and **9/9 CLI** at `2026-09-07T07:46:57.590Z`. They compare rendered registry inference and analytics JSON/timestamps with the actual captures, verify release versions and desktop/mobile navigation, and report no uncaught JavaScript errors or unexpected external requests. The explicitly disabled browser sandbox and origin/font allowlist remain disclosed; no live credentials enter the browser. The SDK's local preview also passes **6/6** at `2026-09-07T07:47:11.222Z`; its owned preview server was stopped after public verification. Source-only documentation/report follow-ups do not change either published runtime package.

The user-prefix `airs` installation reports **4.2.2** and resolves SDK **0.23.0**, with independently verified package bytes. The `2026-09-07T07:50:29.171Z` source/artifact/built-site/registry/installed-CLI secret scan checks **17,359 files**, detects no credential leaks and confirms the read-only configuration is unchanged. Runtime endpoint/key settings were not persisted. Package delivery does not certify all gateway APIs, the failed realtime session, other recorded service failures, the retained DLP fixture, public WAN access or local container execution. Full original-scope assessment remains **5/10**.

The next review found a real response-schema bug: empty SCM latency cohorts return null period aggregates, which SDK 0.22.0 rejects. A failing-first regression reproduces the rejection. SDK 0.23.0 preserves null mean/percentiles, keeps numeric buckets and required fields, and adds verified trace/metadata filters to cost, tokens and latency without widening unrelated chart/group contracts. The existing request-options interface remains available; strict TypeScript consumers must handle nullable latency aggregates.

Raw SCM checks pass **13/13** at `2026-09-07T07:11:38.044Z`. The packed SDK candidate independently passes **13/13** at `2026-09-07T07:16:54.357Z`, testing known and nonexistent cohorts plus combined filters against existing owned traffic. A positive-cost/token record is selected by its journaled retired service-key ID and matching metadata. Early controls correctly rejected zero-token ingestion fixtures; they were replaced by the existing inference record, not newly generated traffic. No runtime key, inference request or audit record was created; credentials remain unchanged.

The full SDK suite passes **10,668 tests in 134 files**, with 99.71% lines/statements, 100% functions and 96.53% branches. The 74 added cases include pre-authentication validation, serialization, nullable-response regression and independently source-hashed partial-adapter contracts. Direct gateway coverage is still **138/242 (57.02%)**; all 22 analytics operations remain partial and all other failures remain disclosed. Full-scope self-assessment remains **5/10**, not an invented 9/10. Publication and installed-release checks are recorded separately after verification.

## Previous delivered packages — SDK 0.22.0 / CLI 4.2.1

Both new releases are published and independently installed from npm. [SDK v0.22.0](https://github.com/cdot65/prisma-airs-sdk/releases/tag/v0.22.0) is tagged at `682690ed2535bff4a4e98c3169836a6bdb51c2ad`; [publication passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34090737587), as did [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34090500445) and the [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34090500424). [CLI v4.2.1](https://github.com/cdot65/prisma-airs-cli/releases/tag/v4.2.1) is tagged at `6c336904c0a2b606e50454f4c749c03ec5ae1153`; [publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34091442894) and all four [CI jobs](https://github.com/cdot65/prisma-airs-cli/actions/runs/34091312075) pass. Both CLI remotes contain the release commit and tag. Versions were cut in SDK-first order; CLI uses Changesets and an exact SDK manifest/lockfile pin.

Registry-installed SDK inference passes **10/10** at `2026-09-07T06:30:31.639Z`. Registry-installed CLI inference passes **8/8** at `2026-09-07T06:38:15.012Z`. The upgraded local user-prefix CLI passes **12/12** cross-service reads/benign scan checks at `2026-09-07T06:38:12.859Z`. Independent retirement audit passes **22/22** at `2026-09-07T06:39:30.865Z`, covering every historically journaled release-inference key. Credentials remain unchanged and runtime settings were not persisted.

SDK registry verification matches all nine package files, 126 source-map files and 1,303 runtime exports. Both ESM/CommonJS formats pass strict consumer types, actual loopback WebSocket exchanges and real 30-second OAuth deadline/recovery tests. Archive SHA-256 is `e0ff62f2f7e4ef8f01e5718ba08de066bfd996d605458b78394ed495463eb04b`; registry integrity matches the candidate. CLI registry and installed-user copies match all seven package files, including five distribution files; version/help and all 19 library exports load. An initial comparison detected pnpm's package-manifest normalization, so verification was repeated against the same npm packaging path used for publication; the earlier archive was preserved.

SDK full coverage passes **10,594 tests**, with 99.71% lines/statements, 100% functions and 96.53% branches. CLI passes **1,033 tests**, 95.96% lines/statements, 95.48% functions and 87.28% branches. Its existing seven lint warnings remain; no error or coverage gate was waived. Main pushes now run all CLI gates, and publication adds format/coverage enforcement.

The SDK adds experimental realtime transport and reaches **138/242 (57.02%)** direct gateway contracts, with 29 experimental methods; AIRS remains **149/149**. The live realtime session still fails with `invalid_model` after HTTP 101, and the actual new runnable example fails too. No alternate provider/model or generation was used. This is **5/10 against the full original scope**, not a 99%/all-green certification. The three original walkthrough failures, backend/authorization gaps, retained DLP fixture and unverified WAN path remain explicit below.

The corrected browser preview passes **6/6** at `2026-09-07T06:29:47.929Z`. Its first run caught a primary/supplemental example-count mismatch; the generator now reports 21 primary scripts plus the independently executed realtime script, preserving separate timestamps and all failures. Public deployment and browser verification of the final registry captures are recorded separately after completion. The new [container publication](https://github.com/cdot65/prisma-airs-cli/actions/runs/34091441230) also passes, but local container execution/public pull remain unverified for the previously documented environment reasons.

### Final public delivery verification

The SDK's corrected registry-output documentation is pushed in `4c833406b3cf265896d1b20115490adcb7141689`: [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34092129359), [Node matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34092129332) and [Docusaurus deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34092129353) all pass. The CLI's final registry capture is pushed to both remotes in `9ecaea2ccf57d79f342ccf03910626630636bf34`; [CI](https://github.com/cdot65/prisma-airs-cli/actions/runs/34092061466) and [deployment](https://github.com/cdot65/prisma-airs-cli/actions/runs/34092061415) pass.

Real Chromium public-page checks pass **8/8 SDK** at `2026-09-07T06:49:29.950Z` and **9/9 CLI** at `2026-09-07T06:45:29.228Z`. They compare rendered JSON and timestamps against the actual registry and realtime captures, verify release versions and desktop/mobile navigation, and detect no uncaught JavaScript errors or unexpected external requests. Browser credentials remain empty; its explicitly disabled sandbox and network allowlist are disclosed in the private reports. The final local preview also passes 6/6. These source-only follow-ups do not alter either published runtime package.

The 06:45:25 UTC source/artifact/built-site/registry/installed-CLI scan checks **12,086 files**, detects no credential leaks and verifies unchanged configuration. The credential file remains read-only; temporary runtime keys are retired. The CLI container's published manifest digest is `sha256:a9f958aa2cb9db67d40cc949d26d094c657a0ad7cdf2df76ccd247a21daeca55`; publication is not local-container or public-pull certification.

The authorized release increment is delivered. The original full-project goal is still incomplete and remains **5/10** for the reasons above; successful transport, packages and documentation do not erase failed provider sessions or missing gateway APIs.

## Original 0.21.0 / 4.2.0 release targets

- SDK: `@cdot65/prisma-airs-sdk@0.21.0`, first.
- CLI: `@cdot65/prisma-airs-cli@4.2.0`, pinned to the published SDK `0.21.0`, then clean-installed and tested.
- GitHub source, releases and both Docusaurus sites must be delivered. The CLI source is also mirrored to its configured origin.

Registry publication is verified for both packages. The SDK was published first; the CLI's manifest and lockfile pin the released SDK. Source and release tags are pushed to GitHub, and CLI source/tags are also pushed to its configured origin. Documentation contains the latest registry-installed E2E output; deployment runs and public pages are linked below.

## Delivery and release verification

- [SDK v0.21.0](https://github.com/cdot65/prisma-airs-sdk/releases/tag/v0.21.0), commit `56bb62593f94931f6ae7b8c05eeacf4e81e32ec3`; [npm publication passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34076775984).
- [CLI v4.2.0](https://github.com/cdot65/prisma-airs-cli/releases/tag/v4.2.0), commit `852401bb41024b5e8e8213d43eed20f89bd1fb3f`; [npm publication passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34077320213). Registry visibility lagged workflow success by several minutes; availability was checked independently before installation.
- SDK clean-checkout CI and Node 18/20/22/24 matrix pass, with 10,483 tests. A fresh CLI checkout using the frozen lockfile passes 1,033 tests, coverage gates, lint/format, typecheck and build. Seven pre-existing CLI lint warnings remain non-fatal.
- Registry SDK payload matches all nine candidate files, 124 source-map sources and 1,301 runtime exports. ESM/CommonJS and strict consumer types pass, including real 30-second OAuth deadline/recovery checks. Archive SHA-256: `dd23e747627606a2ecbcc8c9d130b269c00cc9a09208cfacada28919e897c360`.
- Registry-installed SDK inference: **10/10**, finished `2026-09-07T02:41:08.778Z`. Registry-installed CLI inference: **8/8**, finished `2026-09-07T02:52:01.411Z`.
- Independent dev-workspace key inventory at `2026-09-07T02:53:54.412Z` confirms both release-test keys absent. Credentials were read-only and unchanged.
- [Multi-architecture container publication passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34077318681) for `ghcr.io/cdot65/prisma-airs-cli:4.2.0`, digest `sha256:69f560819b044fd84e9ab488c3ef02f0e3d82981f3e7023165184bf2c06111a2`. This environment has no Docker executable and registry pull authorization returned 403, so local container runtime verification is not claimed. Package visibility and credentials were not changed.
- [SDK published-package examples](https://cdot65.github.io/prisma-airs-sdk/guides/release-verification/) and [CLI inference examples](https://cdot65.github.io/prisma-airs-cli/cli/aigateway/inference/) show sanitized actual output. Source-only documentation follow-ups are on `main`; release tags remain immutable.

The CLI's first documentation build caught a renamed-heading anchor. The existing anchor was restored; strict local build and the corrected remote deployment pass. The link check was not disabled. Initial failed checks remain visible in Actions history.

Detailed local evidence is retained under `artifacts/`, including registry consumer verification, fresh CLI coverage results, release inference, key cleanup and public-browser reports. These private artifacts are intentionally not committed.

## Post-deployment verification

- SDK documentation source `9592ee50ecf7fa2b409b64c58f48fcf2f07eb40c`: [deployment passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671885), [CI passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671880), and [Node matrix passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671878).
- CLI documentation source `c2e43a5e463011cfb56a601dfcaa206e0dc1ce74`: [deployment passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34078257099).
- Public SDK browser checks: **8/8**, `2026-09-07T03:14:53.223Z`. Public CLI browser checks: **9/9**, `2026-09-07T03:11:04.097Z`. Checks verify actual captured JSON and timestamps, release versions, desktop/mobile navigation, and no uncaught JavaScript errors or unexpected external requests. The SDK published-package page matches the registry-installed CLI capture exactly; the earlier 21-script results and failures retain their separate timestamp.
- Chromium `152.0.7977.82` ran from an isolated user-cache installation on Alpine. Its sandbox was explicitly disabled for this container; requests were restricted to the public documentation and configured font assets, and no live credentials were loaded. An initial navigation check observed the URL before React rendered its destination; the verifier now waits for the destination heading before retaining all timestamp/output assertions. The failed report is preserved separately.
- The local user-prefix CLI is installed at `4.2.0`; `airs --version` and inference help pass. Its five distribution files match the clean release build byte-for-byte, and its resolved SDK reports `0.21.0`. Existing global packages and credential configuration were not changed. Interactive inference still requires the explicitly configured runtime endpoint/key; these were not persisted into the read-only config. The release harness supplied temporary runtime keys and verified their retirement.
- Source/artifact/built-site/installed-CLI secret scan at `2026-09-07T03:17:20.687Z`: **16,907 files, zero detected credential leaks**, configuration unchanged.

A subsequent [post-deployment matrix run](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34079148434) exposed a TypeDoc audit timeout in the Node 22 coverage worker: 10,482 tests passed, while the API-example audit exceeded its 60-second budget and stalled a worker RPC. The audit now executes the same strict gate in an asynchronous child process with a 45-second hard deadline, below the unchanged 60-second test budget. No examples or coverage thresholds were excluded. Local full coverage passes all 10,483 tests with 99.71% statements/lines, 100% functions and 96.42% branches; the isolated audit takes 14.13 seconds in that run. A disposable undocumented export independently confirms the child gate still exits 1 and names the missing example. This test-only correction does not alter either published package or deployed page content.

## Realtime implementation increment — 0.22.0

The current increment adds experimental `AIGatewayInferenceClient.connectRealtime()` with an explicit caller-owned Node WebSocket adapter, validated JSON events, bounded queues/deadlines/cancellation and no reconnect, compression or redirect following. Zod remains the only production dependency. The pinned OpenAPI upgrade operation has a separate frozen fixture and real loopback WebSocket tests, rather than an HTTP-204 mock or invented provider-event schema.

Direct AI Gateway implementation increases to **138/242 (57.02%)**, with 29 experimental methods. AIRS remains 149/149. JSON field coverage remains 1,358/1,358 request and 2,329/2,329 response properties; HTTP 101 has no JSON body and is tested separately.

The typed live run ends at `2026-09-07T06:04:42.282Z`: **4 pass / 1 fail**. Same-key model lookup, HTTP 101, socket closure and temporary-key retirement pass; provider session creation fails with `invalid_model`. No client event, generation, audio or alternate model/provider was used. The actual new example was executed against the source checkout separately from the earlier 21-script batch and fails at `2026-09-07T06:12:26.528Z`; its real sanitized output is reproduced in Docusaurus. Independent read-only retirement audit at `2026-09-07T06:13:23.127Z` passes **6/6**, including older failed attempts. The credential source remains unchanged.

Review found that the E2E key selector could choose an MCP-only key for inference after the discovery iteration. It now requires the already established `completions.write` scope by default; explicit MCP runs still select only `mcp.invoke`. No existing key or workspace permission was edited. The initial 403 control failure is retained in history. The release version identity regression also caught an omitted SDK User-Agent version update; the constant was corrected before release. Neither failure is hidden or reclassified as a passing run.

Full-scope self-assessment remains **5/10**. This increment closes a transport implementation gap; it does not resolve provider/model support, authoring, service storage, entitlement or DLP cleanup failures. Publication and final gates are recorded below only when actually verified.

Local 0.22.0 gates pass **10,594 tests in 133 files**, 99.71% statements/lines, 100% functions and 96.53% branches. The new realtime transport has 100% statements/lines/functions and 97.91% branches. Lint, formatting, source/tooling typechecks, strict public API example coverage and the full Docusaurus production build pass. The source-hashed audit confirms 138/242 direct gateway operations without moving the denominator. Production dependency audit reports zero known vulnerabilities. The separately documented Docusaurus development-only advisory remains unresolved, not silently counted as fixed.

## Self-assessment against the original goal

Release delivery and published inference verification are complete; the original full-project target is not. The assessment remains **5/10 against the full scope**, not an invented 9/10 based on successful publication. No all-workflow or WAN-readiness claim is made.

Further acceptance work remains: implement and verify the remaining Prisma-compatible API families without shrinking the 242-operation denominator; establish the unsupported authoring/realtime lifecycles with the required models; resolve the documented service, entitlement and gateway storage failures; retire the unbound DLP fixture; and then rerun affected E2E workflows and refresh their exact documentation captures. These prerequisites require verified service support or deliberate environment changes, not merely another SDK release. The retained assessment and gateway ledger identify the individual failures and evidence.

## Post-release conformance iteration: usage-policy reset

The previous extension suite skipped resetting a usage-policy entity because its isolated policy had received no traffic. This was an E2E fixture gap, not an established service blocker. A new bounded harness supplies exactly one tagged request with the prescribed inference model to a newly created dev-only policy, proves its scope and positive counter, resets that exact entity, verifies zero usage, and archives the policy and retires its temporary key.

Two independent lifecycle runs passed 8/8, at `2026-09-07T03:35:08.959Z` and `2026-09-07T03:39:07.317Z`. Each observed 14 tokens before reset and zero afterward. Separate read-only audits passed 2/2 at `2026-09-07T03:37:38.493Z` and `2026-09-07T03:39:59.498Z`, proving each archived policy's exact identity/workspace and each key's absence from the complete inventory. No existing counter was reset, and the credential source remained unchanged. Historical fixture journals and results are retained.

Twenty offline guard checks reject changed policy IDs, workspace/name/status/type/limit/conditions/grouping, unexpected entity keys and invalid counter values. New frozen live-response structures include the actual `{}` reset acknowledgement and populated entity response. The public [post-release examples](https://cdot65.github.io/prisma-airs-sdk/guides/release-verification/) retain the exact numeric output and distinguish these new live checks from older skips.

The updated local suite passes **10,508 tests in 129 files**, including five additional frozen-response cases and 20 fixture-safety cases. Coverage is 99.71% statements/lines, 100% functions and 96.43% branches. Tooling typecheck, lint and formatting pass. These post-release checks use the source checkout; no runtime implementation differs from the published 0.21.0 SDK.

This is concrete progress toward complete live validation, not new operation coverage: 137/242 remains unchanged. The published SDK 0.21.0 stability annotation remains experimental until a later release explicitly promotes it. No runtime package code or CLI dependency was changed by this verification-only increment. The original full-scope score remains 5/10 while the larger API/service gaps persist.

## Post-release conformance iteration: MCP discovery

An owned discovery harness now exercises the separate, deployment-verified MCP ingress using the official MCP client as an exact-pinned development dependency. It restricts outbound traffic to the exact owned HTTPS endpoint, allows initialization/tool-list protocol messages but never tool execution, rejects redirects, caps responses at 8 MiB, and bounds requests to 20 seconds. Its key requests only the existing `mcp.invoke` scope; SDK production dependencies and runtime code are unchanged.

The initial fixture used a colliding integration/server slug (400). Distinct slugs resolved creation. The detail response omitted optional workspace/integration fields, so the corrected guard verifies those boundaries through workspace-scoped server listing and the integration detail API instead. Those failed attempts remain in history.

Minimal clones of three existing upstreams all reached the gateway but failed initialization. A narrowly scoped diagnostic in the exact deployed `2.20.0` gateway bundle identified an upstream `unauthorized` response behind the generic session-restore HTTP 500. Preserving the source's actual JWT/identity-forwarding/header-passthrough configuration produced the more explicit missing-caller-header HTTP **401**. No unrelated AIRS/SCM secret was substituted and no existing authentication policy was relaxed.

The latest lifecycle at `2026-09-07T04:15:58.219Z` reports **9 pass / 1 fail**, zero baseline capabilities, zero tools invoked and no issued session ID. It does **not** verify `mcpServers.updateCapabilities`, whose contract-only status remains explicit. The independent audit at `2026-09-07T04:16:47.605Z` passes **19/19** across all seven attempts: all 18 journaled resources are absent, and the complete dev inventory contains no implicitly created server attached to an owned integration. Credentials remain unchanged. The [published examples guide](https://cdot65.github.io/prisma-airs-sdk/guides/release-verification/) includes the exact sanitized negative result, rather than presenting this as successful discovery.

This iteration replaces an unexplained missing-tool fixture with a reproducible authentication prerequisite and stronger safeguards. It does not increase 137/242 direct coverage or the full-scope **5/10** assessment. The release remains SDK 0.21.0 / CLI 4.2.0; no new runtime package is warranted by verification-only changes.

The final local suite passes **10,527 tests in 130 files**, with 99.71% statements/lines, 100% functions and 96.42% branches. It includes 13 new MCP safety cases, an additional exact-host DNS case and five additional frozen live-response cases (71 total). Tooling typecheck, lint, formatting, the fresh source-hashed OpenAPI audit, and strict Docusaurus build/typecheck pass. The dependency install audit reports zero known vulnerabilities; the 04:20:32 UTC credential scan checks 17,198 files with zero detected leaks and an unchanged source configuration. No runtime source file changed.

## Post-release conformance iteration: MCP runtime enforcement

The missing MCP tool prerequisite is now resolved for an owned public-reference upstream. The complete lifecycle passes **16/16** at `2026-09-07T05:03:17.184Z`: discovery populated three SCM capabilities, the SDK disabled and re-enabled one, and the runtime tool list changed **3 → 2 → 3** while preserving unrelated tools. Neither this run nor its fixture setup invoked tools. An earlier control-plane-only lifecycle passed 14/14; the subsequent 20-second runtime-enforcement check failed and remains historical evidence.

The final polling window follows the inspected 2.20.0 gateway's 300-second disabled-capability cache, without cache flushing or policy changes. Hidden/restored states were observed at 30.751 / 60.601 seconds respectively; these are polling observation times, not exact latency measurements. This validates eventual enforcement, not an immediate-enforcement promise. Microsoft documents the selected [public MCP reference endpoint](https://learn.microsoft.com/en-us/training/support/mcp) as requiring no authentication. No caller identity, credential or custom-header forwarding was configured for it.

A separate private synthetic upstream exposed an important boundary: gateway SSRF protection rejected its cluster address before any request reached it. Its 15/16 report remains a failed discovery workflow, not a successful capability test. All four bounded, internal-only Kubernetes fixtures were deleted with UID preconditions. An independent audit passes **17/17** at `2026-09-07T05:05:16.697Z` across all 16 journaled synthetic/public-attempt resources and the implicit-server inventory. Authenticated-source HTTP 401 failures remain valid for those integrations and were not bypassed.

The official-client loopback regression also found a test-only cancellation-order bug: aborting native fetch before canceling an empty 202 response body incorrectly rejected initialization. Reader cancellation now starts first, and the coupled protocol regression passes. Production SDK cancellation already handles cleanup failures; it did not require a runtime change. Shared lifecycle logic prevents the source-clone and public/synthetic checks from diverging. API JSDoc now records the newer MCP/usage-reset evidence while retaining experimental status; published packages and their runtime behavior remain unchanged.

The [examples guide](https://cdot65.github.io/prisma-airs-sdk/guides/release-verification/) retains exact positive and negative output, propagation limits and distinct cleanup audits. This closes a live-validation gap, not an additional upstream operation: **137/242** and the full-scope **5/10** assessment remain unchanged. Prompt/resource capability variants, source-specific authentication, the other recorded service failures and the missing upstream families still require work.

Final local verification passes **10,544 tests in 131 files**, including 12 synthetic-fixture boundary/protocol cases and 76 frozen live responses. Coverage is 99.71% statements/lines, 100% functions and 96.43% branches. Lint, formatting, tooling typecheck, the fresh source-hashed OpenAPI audit, and strict Docusaurus build/typecheck pass. The `2026-09-07T05:17:56.258Z` secret scan checks 16,416 source, artifact, built-site and installed-CLI files with zero detected credential leaks and unchanged source credentials. The two runtime-source edits are JSDoc only: their comment-free TypeScript syntax trees match release `v0.21.0`. No new package release or CLI dependency change is needed for this verification increment.

The subsequent [Node matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34086706657) caught a Node 18 compatibility error in the new synthetic fixture: its development-only MCP server dependency calls global `crypto.randomUUID()`, but Node 18 does not expose that global by default. The fixture now supplies `node:crypto`'s native Web Crypto only when absent, preserving existing implementations. The same error was reproduced locally before correction. Both existing/absent-global protocol regressions now pass, as do all **10,545 tests** on local Node 18 and the full coverage suite with unchanged thresholds. No production SDK implementation, runtime policy, or successful live-run capture changed; the failed matrix run remains historical evidence.

Delivery is independently confirmed: source `ceaf0d84eaea7890ba7ff09ec6f7ddf123b57a1c` passes [CI](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34087121943) and the complete [Node 18/20/22/24 matrix](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34087122018). The [SDK documentation deployment](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34086706620) from `ccf554f` passes; the later fixture-only correction does not change page content. Fresh public-browser checks pass **8/8 SDK** at `2026-09-07T05:30:56.588Z` and **9/9 CLI** at `2026-09-07T05:32:03.362Z`. They compare the rendered examples against the captured positive/negative MCP, usage-reset and registry-inference results. Both CLI remotes remain synchronized at `c2e43a5`; npm and the installed CLI still report SDK 0.21.0 / CLI 4.2.0. This verification increment is delivered, separately from the unmet full-project target.

## Retained pre-release evidence

- 10,483 SDK tests pass on Node 18/20/22/24; 1,033 CLI tests pass against the local candidate.
- AIRS measured operations: 149/149. AI Gateway direct upstream contracts: 137/242 (56.61%); 28 experimental methods.
- Live inference: SDK 10/10 and CLI 8/8 with the prescribed models.
- Primary examples: 18 passing, 3 failing scripts; all 21 executed. DLP profile update, dictionary upload and network-broker update fail.
- Cleanup: 341/342 aggregate checks; one unbound DLP profile remains active after service-side retirement failures.
- Authentication/provisioning gaps, runtime log-detail/feedback HTTP 500s, unsupported runtime families and WAN connectivity remain explicitly uncertified.
- See [the historical assessment](SDK-ASSESSMENT.md), [example output](docs-site/docs/guides/examples.mdx) and [live results](docs-site/docs/developer/live-validation-results.md). Private credentials, fixture journals and raw artifacts are excluded from Git and npm.
