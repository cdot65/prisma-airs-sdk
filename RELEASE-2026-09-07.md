# September 7, 2026 release delivery

The user explicitly authorized pushing and publishing the implemented SDK/CLI surface with the remaining AI Gateway gaps disclosed. This supersedes the earlier decision to withhold all releases; it does not change the original 99% goal or turn failed E2E workflows into passes.

## Release targets

- SDK: `@cdot65/prisma-airs-sdk@0.21.0`, first.
- CLI: `@cdot65/prisma-airs-cli@4.2.0`, pinned to the published SDK `0.21.0`, then clean-installed and tested.
- GitHub source, releases and both Docusaurus sites must be delivered. The CLI source is also mirrored to its configured origin.

Registry publication is verified for both packages. The SDK was published first; the CLI's manifest and lockfile pin the released SDK. Source and release tags are pushed to GitHub, and CLI source/tags are also pushed to its configured origin. Documentation contains the latest registry-installed E2E output; deployment runs and public pages are linked below.

## Delivery and release verification

- [SDK v0.21.0](https://github.com/cdot65/prisma-airs-sdk/releases/tag/v0.21.0), commit `56bb62593f94931f6ae7b8c05eeacf4e81e32ec3`; [npm publication passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34076775984).
- [CLI v4.2.0](https://github.com/cdot65/prisma-airs-cli/releases/tag/v4.2.0), commit `852401bb41024b5e8e8213d43eed20f89bd1fb3f`; [npm publication passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34077320213). Registry visibility lagged workflow success by several minutes; availability was checked independently before installation.
- SDK clean-checkout CI and Node 18/20/22/24 matrix pass, with 10,483 tests. A fresh CLI checkout using the frozen lockfile passes 1,033 tests, coverage gates, lint/format, typecheck and build. Seven pre-existing CLI lint warnings remain non-fatal.
- Registry SDK payload matches all nine candidate files, 124 source-map sources and 1,301 runtime exports. ESM/CommonJS and strict consumer types pass, including real 30-second OAuth deadline/recovery checks. Archive SHA-256: `dd23e747627606a2ecbcc8c9d130b269c00cc9a09208cfacada28919e897c360`.
- Registry-installed SDK inference: **10/10**, finished `2026-09-07T02:41:08.778Z`. Registry-installed CLI inference: **8/8**, finished `2026-09-07T02:52:01.411Z`.
- Independent dev-workspace key inventory at `2026-09-07T02:53:54.412Z` confirms both release-test keys absent. Credentials were read-only and unchanged.
- [Multi-architecture container publication passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34077318681) for `ghcr.io/cdot65/prisma-airs-cli:4.2.0`, digest `sha256:69f560819b044fd84e9ab488c3ef02f0e3d82981f3e7023165184bf2c06111a2`. This environment has no Docker executable and registry pull authorization returned 403, so local container runtime verification is not claimed. Package visibility and credentials were not changed.
- [SDK published-package examples](https://cdot65.github.io/prisma-airs-sdk/guides/release-verification/) and [CLI inference examples](https://cdot65.github.io/prisma-airs-cli/cli/aigateway/inference/) show sanitized actual output. Source-only documentation follow-ups are on `main`; release tags remain immutable.

The CLI's first documentation build caught a renamed-heading anchor. The existing anchor was restored; strict local build and the corrected remote deployment pass. The link check was not disabled. Initial failed checks remain visible in Actions history.

Detailed local evidence is retained under `artifacts/`, including registry consumer verification, fresh CLI coverage results, release inference, key cleanup and public-browser reports. These private artifacts are intentionally not committed.

## Post-deployment verification

- SDK documentation source `9592ee50ecf7fa2b409b64c58f48fcf2f07eb40c`: [deployment passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671885), [CI passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671880), and [Node matrix passed](https://github.com/cdot65/prisma-airs-sdk/actions/runs/34078671878).
- CLI documentation source `c2e43a5e463011cfb56a601dfcaa206e0dc1ce74`: [deployment passed](https://github.com/cdot65/prisma-airs-cli/actions/runs/34078257099).
- Public SDK browser checks: **8/8**, `2026-09-07T03:14:53.223Z`. Public CLI browser checks: **9/9**, `2026-09-07T03:11:04.097Z`. Checks verify actual captured JSON and timestamps, release versions, desktop/mobile navigation, and no uncaught JavaScript errors or unexpected external requests. The SDK published-package page matches the registry-installed CLI capture exactly; the earlier 21-script results and failures retain their separate timestamp.
- Chromium `152.0.7977.82` ran from an isolated user-cache installation on Alpine. Its sandbox was explicitly disabled for this container; requests were restricted to the public documentation and configured font assets, and no live credentials were loaded. An initial navigation check observed the URL before React rendered its destination; the verifier now waits for the destination heading before retaining all timestamp/output assertions. The failed report is preserved separately.
- The local user-prefix CLI is installed at `4.2.0`; `airs --version` and inference help pass. Its five distribution files match the clean release build byte-for-byte, and its resolved SDK reports `0.21.0`. Existing global packages and credential configuration were not changed.
- Final source/artifact/built-site secret scan at `2026-09-07T03:09:54.626Z`: **16,890 files, zero detected credential leaks**, configuration unchanged.

## Self-assessment against the original goal

Release delivery and published inference verification are complete; the original full-project target is not. The assessment remains **5/10 against the full scope**, not an invented 9/10 based on successful publication. No all-workflow or WAN-readiness claim is made.

Further acceptance work remains: implement and verify the remaining Prisma-compatible API families without shrinking the 242-operation denominator; establish the unsupported authoring/realtime lifecycles with the required models; resolve the documented service, entitlement and gateway storage failures; retire the unbound DLP fixture; and then rerun affected E2E workflows and refresh their exact documentation captures. These prerequisites require verified service support or deliberate environment changes, not merely another SDK release. The retained assessment and gateway ledger identify the individual failures and evidence.

## Retained pre-release evidence

- 10,483 SDK tests pass on Node 18/20/22/24; 1,033 CLI tests pass against the local candidate.
- AIRS measured operations: 149/149. AI Gateway direct upstream contracts: 137/242 (56.61%); 28 experimental methods.
- Live inference: SDK 10/10 and CLI 8/8 with the prescribed models.
- Primary examples: 18 passing, 3 failing scripts; all 21 executed. DLP profile update, dictionary upload and network-broker update fail.
- Cleanup: 341/342 aggregate checks; one unbound DLP profile remains active after service-side retirement failures.
- Authentication/provisioning gaps, runtime log-detail/feedback HTTP 500s, unsupported runtime families and WAN connectivity remain explicitly uncertified.
- See [the historical assessment](SDK-ASSESSMENT.md), [example output](docs-site/docs/guides/examples.mdx) and [live results](docs-site/docs/developer/live-validation-results.md). Private credentials, fixture journals and raw artifacts are excluded from Git and npm.
