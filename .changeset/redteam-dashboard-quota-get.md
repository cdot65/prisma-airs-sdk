---
'@cdot65/prisma-airs-sdk': minor
---

Add the Red Team dashboard GET quota method without changing the existing POST contract;
validate all seven dashboard data feeds using fresh OAuth and aggregate-only evidence.
