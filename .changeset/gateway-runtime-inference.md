---
'@cdot65/prisma-airs-sdk': minor
---

Add explicitly configured gateway runtime inference with separate key authentication, typed
chat/Responses/embedding APIs, bounded cancellable SSE, and typed file/model/batch/vector-store/fine-tuning
resources. Preserve SCM OAuth defaults. Record live compatibility corrections, offline contracts,
experimental lifecycle methods and verified SDK/CLI inference results. Automatic runtime retries
default to zero to avoid duplicate generations or resource creation.

Fine-tuning listing is live-verified. Job creation, retrieval, cancellation, events and checkpoints
have offline contracts only; no training was started or model selection changed for verification.
