---
'@cdot65/prisma-airs-sdk': minor
---

Add the SCM admin-plane secretReferences client with typed list/create/get/update/delete operations. Verify the unbound synthetic-reference lifecycle against dev, validate credential discriminators and workspace access conflicts before network I/O, and omit authentication configuration from debug bodies with shared CLI redaction metadata. External secret-manager resolution is not certified by CRUD tests.

Reject URL-normalized dot-segment identifiers in generated SCM MCP, usage/rate policy, log-export and secret-reference clients before authentication or network access.
