---
'@cdot65/prisma-airs-sdk': minor
---

Add experimental runtime feedback-update and log-detail methods with strict inputs,
typed additive responses, conditional v2 log timestamp validation, and no automatic
retries by default. Both routes are confirmed, but successful live execution remains
blocked by the deployed gateway's storage handlers. No storage configuration is changed.
