---
'@cdot65/prisma-airs-sdk': minor
---

Add experimental runtime legacy completions, prompt completions and prompt rendering with strict typed requests, additive response validation and bounded cancellable SSE. Flatten prompt hyperparameters according to the pinned source's explicit wire instructions, preserve both native and documented wrapped JSON completion results, and retain nullable intermediate legacy completion state.

Both deployed gateway replicas register these routes. Offline contracts and packaged JSON/SSE/TypeScript checks are not successful live certification: the prescribed model rejects legacy completion, and no owned prompt template has been provisioned. No SCM authoring route, model, credential mode or production configuration is silently substituted.
