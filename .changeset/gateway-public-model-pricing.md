---
'@cdot65/prisma-airs-sdk': minor
---

Add an explicitly configured, unauthenticated public model-pricing client with typed catalog
rates and unevaluated calculation expressions. Keep public lookup routing separate from SCM
OAuth and gateway inference keys; validate inputs before I/O and preserve caller cancellation,
bounded deadlines, zero default retries and additive response compatibility. Catalog rates
are in USD cents and are not effective tenant billing.
