---
'@cdot65/prisma-airs-sdk': patch
---

Add 4 missing fields from live API to security profile schemas: `csp_id` and `tsg_id` on `SecurityProfile`, `options` on `ModelProtectionItem`, `description` on `DlpDataProfilePolicy`.
